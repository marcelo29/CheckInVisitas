<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="android.view.View" />
    </data>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:theme="@style/AppTheme.LoginFloatingActionBottom"
        tools:context="br.com.android.check.fragment.VisitaFragment">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_lst"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="horizontal" />

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fabAtualizaLista"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_alignBottom="@id/rv_lst"
            android:layout_alignEnd="@id/rv_lst"
            android:layout_alignRight="@id/rv_lst"
            android:layout_marginEnd="0dp"
            android:layout_marginRight="60dp"
            android:onClick="atualizaLista"
            android:src="@drawable/ic_playlist_white"
            android:visibility="invisible" />

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fabFinalizaVisita"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_alignBottom="@id/rv_lst"
            android:layout_alignEnd="@id/rv_lst"
            android:layout_alignRight="@id/rv_lst"
            android:src="@drawable/ic_cad_padrao_branco" />
    </RelativeLayout>
</layout>